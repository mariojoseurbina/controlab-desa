# Tareas de Implementación: Instalador Único y Servidor Unificado

- [x] Configurar Express (`backend/server.js`) para servir los archivos estáticos de React (`frontend/build`).
- [x] Compilar el Frontend de React en producción (`npm run build`) configurando la API relativa `/api`.
- [x] Configurar el empaquetado offline de `node_modules` en el instalador (evitando errores de Prisma/PKG).
- [x] Crear el script de configuración de Inno Setup `controlab-installer.iss` en la raíz del proyecto.
- [/] Validar el instalador compilando el script en Inno Setup GUI.
